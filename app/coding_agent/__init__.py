"""Interactive coding-agent mode (Phase 2).

Independent from app.conversation_manager's Jarvis chat persona — this
package implements a separate plan/act/observe/iterate loop, its own tool
set (file I/O + shell), and its own HTTP surface under /api/agent/*.
"""
