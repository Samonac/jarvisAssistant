# ============================================================================
# J.A.R.V.I.S. Assistant - Python Dependencies
# Install: pip install -r requirements.txt
# ============================================================================

# --- Core Framework ---
flask>=3.0,<4.0
werkzeug>=3.0
python-dotenv>=1.0
requests>=2.28

# --- LLM Providers ---
# (No SDK needed — uses raw HTTP requests to Groq/HuggingFace/Gemini APIs)

# --- Web Search ---
ddgs>=9.0                        # DuckDuckGo search (renamed from duckduckgo-search)
duckduckgo-search>=4.0           # Legacy fallback

# --- Calendar Integration (optional) ---
caldav>=1.3                      # CalDAV calendar support
google-api-python-client>=2.100  # Google Calendar API
google-auth>=2.23                # Google OAuth2
google-auth-oauthlib>=1.1        # Google OAuth2 helper

# --- File Processing ---
openpyxl>=3.1                    # XLSX export and Excel file reading
PyPDF2>=3.0                      # PDF text extraction (optional)
python-docx>=0.8                 # DOCX text extraction (optional)
Pillow>=10.0                     # Image processing for OCR
pytesseract>=0.3                 # OCR via Tesseract (requires tesseract-ocr system package)
# easyocr>=1.7                   # Alternative OCR (heavier, uncomment if needed)

# --- Bluetooth (optional) ---
bleak>=0.21                      # BLE (Bluetooth Low Energy) scanning & connections

# --- SSH (optional) ---
paramiko>=3.0                    # SSH remote command execution

# --- Testing ---
hypothesis>=6.0                  # Property-based testing
pytest>=7.0                      # Test runner
pytest-flask>=1.0                # Flask test helpers
