# SMTP server host (Leave empty to disable email)
SMTP_HOST=

# SMTP server port (default: 587)
SMTP_PORT=587

# SMTP authentication username
SMTP_USERNAME=

# SMTP authentication password
SMTP_PASSWORD=

# From address for outgoing emails
SMTP_FROM_ADDRESS=